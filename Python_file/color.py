class Color:
    def __init__(self, light, dark):
        """
        Initialize a Color object with light and dark color values.

        Args:
            light (tuple): RGB tuple representing the light color.
            dark (tuple): RGB tuple representing the dark color.
        """
        self.light = light
        self.dark = dark
